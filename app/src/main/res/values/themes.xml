<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.EMessengerApplication" parent="Theme.AppCompat.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
        <!-- <item name="colorPrimary">@color/my_light_primary</item> -->
        <item name="android:fontFamily">@font/roboto_text</item>

    </style>

    <style name="Theme.EMessengerApplication" parent="Base.Theme.EMessengerApplication" />

    <style name="Mess.IconButton" parent="@style/Widget.Material3.Button.IconButton.Filled.Tonal">
        <item name="iconTint">@color/primary</item>
        <item name="iconGravity">textStart</item>
    </style>
</resources>